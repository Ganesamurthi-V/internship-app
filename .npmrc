# React Native / Metro cannot resolve pnpm's symlinked store layout.
# Hoisted node_modules is the supported configuration for Expo in a pnpm workspace.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
