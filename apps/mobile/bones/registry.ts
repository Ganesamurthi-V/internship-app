/**
 * Boneyard bones registry.
 *
 * This file is auto-populated by `npx boneyard-js build --native` when running
 * the app on a device. It registers captured bone layouts so the <Skeleton>
 * components can render them without a network call.
 *
 * Import this once in the app entry (_layout.tsx).
 */

// Bones will be registered here by the boneyard CLI.
// Until bones are captured, the Skeleton component falls back to its
// built-in placeholder rendering (the Bone shapes we defined inline).
export {};
